**Instructions for running the scripts:**

1. Compile decodeSopt.f90. 
2. Compile qmaker.f90 (file to generate the qfiles along specific directions in the reciprocal space).
3. You need to update mypath in sqw_map.sh to the directory where col2arr.awk and flip.awk are stored.
4. You may need to install sed, ack and gnuplot in your operating system for plotsqw_course to run.
